{
  "name": "monad-order-storage-kernel",
  "version": "0.4.1",
  "description": "Custody-free FIFO order-storage Solidity library with generation handles and bounded fill receipts",
  "license": "MIT",
  "private": true,
  "files": [
    "contracts",
    "src",
    "README.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "exports": {
    "./order-ref": "./src/order-ref.cjs"
  },
  "scripts": {
    "test": "node scripts/verify-source-lock.cjs && bash verify_and_run.sh && node test/kernel.cjs && node scripts/metropolis-demo.cjs && node scripts/verify-metropolis-evidence.cjs",
    "test:source": "node scripts/verify-source-lock.cjs",
    "demo:metropolis": "npm --prefix consumer install --ignore-scripts --no-audit --no-fund && node scripts/metropolis-demo.cjs",
    "evidence:metropolis": "node scripts/verify-metropolis-evidence.cjs",
    "replay:testnet": "npm --prefix consumer install --ignore-scripts --no-audit --no-fund && node scripts/replay-public-testnet.cjs",
    "pack:preview": "npm pack --ignore-scripts"
  },
  "engines": {
    "node": ">=20"
  }
}
